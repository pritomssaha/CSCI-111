#include <iostream>
using namespace std;
int main() {
  int row=0;
  cout<<"How many rows: ";
  cin>>row;
  for (int i=1;i<=row;i++){
    for(int j=1;j<=2*row-1;j++){
      if(j>=row+1-i && j<=row-1+i ){
        cout<<"*";
      }
      else
      cout<<" ";
    }
    cout<<endl;
  }
  return 0;
}
